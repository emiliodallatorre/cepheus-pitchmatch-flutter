name: cepheus_pitchmatch_flutter
description: Un'applicazione che consente di localizzare le persone nei dintorni per gusti musicali!


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 0.0.1+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Grafica
  swatcher: ^0.0.2
  cupertino_icons: ^1.0.1
  google_fonts: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true